package com.stayserver.stayserver.repository.jpa;

import com.stayserver.stayserver.entity.ItemShare;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ItemShareRepository extends JpaRepository<ItemShare, Integer> {
}
